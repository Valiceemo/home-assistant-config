######################################################################
##  Away Mode
######################################################################

- alias: Away Mode On
  initial_state: 'on'
  hide_entity: False
  trigger:
    - platform: state
      entity_id: input_select.home_mode
      to: 'Away'

  action:
    - service: automation.turn_on
      entity_id: automation.intruder
    - service: automation.turn_off
      entity_id: automation.motion_lights_bedroom_full
    - service: automation.turn_off
      entity_id: automation.bedroom_lights_off_after_10
    - service: light.turn_on
      data:
        entity_id: 
          - light.sensor_node_lounge_led
        brightness: 255
        color_name: green

#####################################################################
##  Home Mode
######################################################################

- alias: Home Mode On
  initial_state: 'on'
  hide_entity: False
  trigger:
    - platform: state
      entity_id: input_select.home_mode
      to: 'Home'
      
  action:
    - service: automation.turn_off
      entity_id: automation.intruder
    - service: automation.turn_on
      entitiy_id: automation.motion_lights_bedroom_full
    - service: automation.turn_on
      entity_id: automation.bedroom_lights_off_after_10
    - service: light.turn_off
      entity_id: light.sensor_node_lounge_led

######################################################################
##  Sleep Mode
######################################################################

- alias: Night Mode
  initial_state: 'on'
  hide_entity: False
  trigger:
    - platform: time
      at: '23:01:00'
    - platform: state
      entity_id: input_select.home_mode
      to: 'Night'
      
  condition:
    - condition: state
      entity_id: device_tracker.rich_s8
      state: 'home'
      
  action:
    - service: automation.turn_off
      entity_id: automation.motion_lights_bedroom_full
    - service: light.turn_off
      entity_id: group.all_lights
    - service: switch.turn_off
      entity_id: switch.tv_backlight_power
      

######################################################################
##  Morning Mode
######################################################################

- alias: Morning Mode
  initial_state: 'on'
  hide_entity: False
  trigger:
    - platform: time
      at: '08:11:00'
      
  condition:
    - condition: state
      entity_id: device_tracker.rich_s8
      state: 'home'
      
  action:
    - service: input_select.select_option
      entity_id: input_select.home_mode
      data:
        option: 'Home'
